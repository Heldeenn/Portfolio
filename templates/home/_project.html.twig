<section class="container project mt-5">
    <h2 class="text-center">Mes différents projets</h2>
    <div class="d-flex flex-wrap justify-content-between mt-4">
        {% for project in projects %}
            <div class="card w-25 my-4 mx-3">
                <img class="card-img-top" src="{{ vich_uploader_asset(project, 'thumbnailFile') }}" alt="{{ project.thumbnail }}">
                <div class="card-body">
                    <h3 class="card-title">{{ project.name }}</h3>
                    <a href="#" class="btn btn-outline-primary">En savoir plus</a>
                </div>
            </div>
        {% endfor %}
    </div>
</section>
