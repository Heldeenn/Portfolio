{% extends 'layout.html.twig' %}

{% block title %}Image{% endblock %}

{% block main %}
<section class="container my-4">
    <h1>Image</h1>
    <div class="text-center">
        <img src="{{ image.name }}" alt="{{ image.name }}">
    </div>


    {{ include('image/_delete_form.html.twig') }}
</section>
    <div class="container">
        <a class="btn btn-primary mx-3" href="{{ path('image_edit', {'id': image.id}) }}">Modifier</a>
        <a class="btn btn-outline-primary" href="{{ path('image_index') }}">Retour</a>
    </div>
{% endblock %}
