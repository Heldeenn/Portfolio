{% extends 'layout.html.twig' %}

{% block title %}Image{% endblock %}

{% block main %}
<section class="container my-4 admin-image-show">
    <h1>Image</h1>
    <div class="text-center mb-3">
        <img src="{{ vich_uploader_asset(image, 'nameFile')}}" alt="{{ image.name }}">
    </div>


    {{ include('image/_delete_form.html.twig') }}
</section>
    <div class="container">
        <a class="btn btn-primary mx-3 mb-4" href="{{ path('admin_image_edit', {'id': image.id}) }}">Modifier</a>
        <a class="btn btn-outline-primary mb-4" href="{{ path('admin_image_index') }}">Retour</a>
    </div>
{% endblock %}
